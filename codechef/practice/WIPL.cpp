#include<bits/stdc++.h>
#define ll long long int
using namespace std;

int main(){
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    
    int t;
    cin>>t;
    while(t--){
        int n, k, temp;
        cin>>n>>k;
        vector<int>h;
        for(int i=0; i<n; ++i){
            cin>>temp;
            h.push_back(temp);
        }
        int h1=0, h2=0;
        sort(h.begin(), h.end());
        for(int i =0; i< n; ++i){
            
        }
        
    }
    return 0;
}