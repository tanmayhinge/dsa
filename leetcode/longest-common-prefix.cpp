#include <bits/stdc++.h>
using namespace std;

class Solution {
public:
    string longestCommonPrefix(vector<string>& strs) {
        string ret = "";

        string min;
        for(int i = 0; i<strs.size(); i++){
            min = 
        }

        return ret;
    }
};


int main(){
    Solution s;
    vector<string> v = {"abc", "ab", "abcde", "abcd"};
    cout<< s.longestCommonPrefix(v);
}